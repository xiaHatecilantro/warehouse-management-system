<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.convenience.store.dao.UserDao">
    <resultMap id="UserResultMap" type="com.convenience.store.entity.User">
        <id property="userId" column="user_id" />
        <result property="username" column="username" />
        <result property="password" column="password" />
        <result property="fullName" column="full_name" />
        <result property="email" column="email" />
        <result property="phone" column="phone" />
        <result property="role" column="role" />
        <result property="status" column="status" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <select id="getUserById" parameterType="int" resultMap="UserResultMap">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>

    <select id="getUserByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM users WHERE username = #{username}
    </select>

    <select id="getAllUsers" resultMap="UserResultMap">
        SELECT * FROM users
    </select>

    <insert id="insertUser" parameterType="com.convenience.store.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (username, password, full_name, email, phone, role, status)
        VALUES (#{username}, #{password}, #{fullName}, #{email}, #{phone}, #{role}, #{status})
    </insert>

    <update id="updateUser" parameterType="com.convenience.store.entity.User">
        UPDATE users
        SET username = #{username}, password = #{password}, full_name = #{fullName}, email = #{email}, 
            phone = #{phone}, role = #{role}, status = #{status}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM users WHERE user_id = #{userId}
    </delete>

    <delete id="batchDeleteUsers" parameterType="list">
        DELETE FROM users WHERE user_id IN
        <foreach item="userId" collection="list" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <select id="searchUsers" parameterType="map" resultMap="UserResultMap">
        SELECT * FROM users
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>
</mapper>